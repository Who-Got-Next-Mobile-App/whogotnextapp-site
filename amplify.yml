version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22 || nvm install 22
        - node -v
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .
    files:
      - .next/**/*
      - node_modules/**/*
      - public/**/*
      - package.json
      - next.config.mjs
  cache:
    paths:
      - node_modules/**/*
